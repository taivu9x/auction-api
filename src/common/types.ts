export enum StatusEnum {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  DELETED = 'deleted',
}

export enum TypeFilter {
  ONGOING = 'ongoing',
  COMPLETED = 'completed',
}

export enum TypeEnum {
  PUBLISH = 'publish',
  DRAFT = 'draft',
}
